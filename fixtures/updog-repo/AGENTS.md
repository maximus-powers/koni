# Fixture Project Instructions

Use pnpm scripts for tests, typechecking, and linting. Keep changes inside the
ticket's declared paths and treat `.codex/koni/` as read-only project policy
during ordinary ticket work.
