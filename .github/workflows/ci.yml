name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  rust:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@v2
      - name: Formatting
        if: matrix.os == 'ubuntu-latest'
        run: cargo fmt --all --check
      - name: Clippy
        if: matrix.os == 'ubuntu-latest'
        run: cargo clippy --workspace --all-targets --locked -- -D warnings
      - name: Tests
        if: matrix.os == 'macos-latest'
        env:
          RUST_TEST_THREADS: 1
        run: cargo test --workspace --locked
      - name: Unsupported-platform safety
        if: matrix.os == 'ubuntu-latest'
        run: >-
          cargo test -p koni-core --test research_profile
          automatic_gate_compile_fails_closed_without_supported_os_sandbox --locked
      - name: Brand audit
        if: matrix.os == 'ubuntu-latest'
        run: ./scripts/check-brand.sh

  docs:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/docs
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/docs/package-lock.json
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run build
      - run: npm audit --audit-level=moderate

  supply-chain:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check

  homebrew:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: brew style packaging/homebrew/koni.rb
